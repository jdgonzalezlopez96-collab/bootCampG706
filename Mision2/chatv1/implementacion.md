# Guía de implementación del chatbot(supervisado)
`python -m venv env312` -> crear el entorno virtual de python dentro del directorio Mision2.  
``env312\Scripts\activate`` -> Activar el entorno virtual de python.  
``python -m pip install nltk`` -> instalar librería nltk

python
import nltk
nltk.download('punkt')

instalar librerías
``pip install scikit-learn nltk numpy  gym==0.26.2 gym-notices``

